name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Generate Missing Environment Files
      run: |
        touch .env
        touch .env.example

    - name: Build Debug APK
      run: |
        gradle wrapper
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon -Djava.awt.headless=true -Dksp.incremental=false -Dksp.keep.java.ast=false --stacktrace

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TutorFee-APK
        path: app/build/outputs/apk/debug/*.apk
